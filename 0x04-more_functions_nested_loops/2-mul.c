#include "main.h"
/**
 * mul - multiplies two integers
 *
 * @a: first parameter
 * @b: second parameter
 *
 * Return: returns the multiplication of two numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
