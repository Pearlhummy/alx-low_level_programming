#include "main.h"
/**
 * factorial - The function to print factorial
 * @n: Parameter
 * Return: Returns factorial
 */
int factorial(int n)
{
	if (n == 0)
	{
		return (1);
	}
	else if (n < 0)
	{
		return (-1);
	}
	else
	{
		return (n * factorial(n - 1));
	}
}
